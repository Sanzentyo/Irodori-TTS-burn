# User Input: Session Handoff (2026-07-21)

現在GPUが占有されているため、セッションを別の端末へ移動します。この際に、齟齬がないように、あなたのコンテキストをちゃんと引き継げるようにdocsの更新をしてください。macやwindowsも移動先の可能性として存在します。benchmarkを取る時は、デバイスのspecや使用状況を必ず確認し、timeoutも設定するようにしてください。wgpuの最適化を引き続きやらせます。最後のbenchmarkは信用にならないので破棄して、そこから再開(デバイスが変わるので、benchmarkのmarkdownはdeviceのspecの名前のものにし、毎回確認して適切な場所に書き込むようにする)するようにdocsを更新しなさい・rubberduckやskillsのload、docsのreadとupdate(sync)などの方針も変わりません。この整備が整ったら、commitしてpushします。benchmarkについては、markdownのgit mvによるrenameが必要で、再開時に別のデバイスなら、benchmarkを全て回し直して分析する過程を挟むようにしなさい
